interface:
  display_name: "Cairn Context"
  short_description: "Preserve durable project decisions across agent sessions"
  default_prompt: "Use $cairn-context to check relevant project decisions before continuing."
policy:
  allow_implicit_invocation: true
